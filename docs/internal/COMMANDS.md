# Obsolete note

This file described legacy `rule1_code` / `rule2_code` entrypoints that **have been removed** from the public artifact.

Use the repository root **`README.md`** and **`docs/REPRODUCIBILITY.md`** for current commands (`scripts/run_single.py`, `scripts/run_all_5seeds.py`, etc.).
