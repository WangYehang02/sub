# Unified Config Index (Non-breaking)

This folder is an index layer for open-source readability.

Existing runtime configs remain in-place under:

- `rule1_code/configs/`
- `rule2_code/configs/`

To avoid changing behavior, this directory currently provides mapping files only.
