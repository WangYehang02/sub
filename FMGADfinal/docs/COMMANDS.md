# Reproducibility Commands (Current Non-breaking Entrypoints)

## Rule1

`python scripts/rule1/train.py --help`

## Rule2

`python scripts/rule2/train.py --help`
`python scripts/rule2/eval_best.py --help`

These commands delegate to original scripts in `rule1_code/` and `rule2_code/`.
Behavior is preserved by design.
